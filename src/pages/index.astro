---
import Layout from "../layouts/Layout.astro";

const services = [
  {
    number: "01",
    title: "Sistemas de gestión a medida",
    solves: "Reemplaza planillas, mensajes y tareas duplicadas por un flujo centralizado.",
    includes: "Módulos operativos, roles, historial, reportes y documentación.",
    ideal: "Empresas con procesos propios que un software genérico no contempla.",
  },
  {
    number: "02",
    title: "Aplicaciones móviles",
    solves: "Lleva una operación, servicio o producto al teléfono de sus usuarios.",
    includes: "App Flutter, autenticación, datos, notificaciones e integración con APIs.",
    ideal: "Negocios que necesitan una experiencia móvil real, no sólo una web reducida.",
  },
  {
    number: "03",
    title: "Paneles y dashboards",
    solves: "Convierte datos dispersos en seguimiento, control y decisiones más claras.",
    includes: "Indicadores, filtros, permisos, exportaciones y vistas administrativas.",
    ideal: "Equipos que necesitan ver el estado de la operación sin armar reportes a mano.",
  },
  {
    number: "04",
    title: "Automatización de procesos",
    solves: "Reduce carga repetitiva, errores de copia y pasos que hoy dependen de una persona.",
    includes: "Formularios, validaciones, integraciones, alertas y tareas programadas.",
    ideal: "Áreas administrativas con circuitos repetibles y reglas conocidas.",
  },
  {
    number: "05",
    title: "Migración y modernización web",
    solves: "Actualiza sitios lentos, difíciles de editar o que ya no representan al negocio.",
    includes: "Arquitectura, contenido, responsive, SEO técnico y despliegue.",
    ideal: "Empresas con una web antigua o una plataforma que necesita una base mantenible.",
  },
  {
    number: "06",
    title: "MVPs para validar una idea",
    solves: "Permite probar el flujo central antes de invertir en un producto completo.",
    includes: "Alcance inicial, prototipo funcional, datos, deploy y próximos pasos.",
    ideal: "Startups y negocios que necesitan aprender con una primera versión usable.",
  },
  {
    number: "07",
    title: "Backend e integraciones",
    solves: "Conecta usuarios, datos y sistemas sin exponer credenciales ni perder control.",
    includes: "Supabase, PostgreSQL, APIs, autenticación, RLS y Cloudflare Workers.",
    ideal: "Productos que necesitan una base segura o integrar herramientas existentes.",
  },
];

const problems = [
  { title: "Planillas desordenadas", problem: "Versiones duplicadas, carga manual y poca trazabilidad.", solution: "Un sistema central con reglas, estados e historial." },
  { title: "Datos repartidos", problem: "Información entre WhatsApp, papel, Excel y distintas personas.", solution: "Una fuente de datos accesible según roles y necesidades." },
  { title: "Poco control operativo", problem: "Horas, tareas, clientes o stock difíciles de seguir.", solution: "Paneles y alertas que muestran qué pasó y qué falta." },
  { title: "Una web que quedó atrás", problem: "El sitio es lento, no convierte o cuesta actualizarlo.", solution: "Una base moderna, rápida y orientada a consultas reales." },
  { title: "Una idea todavía incierta", problem: "No conviene financiar el producto completo sin validarlo.", solution: "Un MVP acotado para probar el flujo de mayor valor." },
  { title: "Sistemas sin permisos claros", problem: "Accesos amplios, pasos opacos y cambios sin registro.", solution: "Autenticación, roles, RLS y trazabilidad desde la arquitectura." },
];

const workSteps = [
  { title: "Relevamiento", text: "Entender el problema, el proceso actual, sus usuarios y las restricciones reales." },
  { title: "Propuesta y alcance", text: "Acordar objetivos, entregables, hitos y criterios de aceptación antes de desarrollar." },
  { title: "Desarrollo visible", text: "Construir por etapas y revisar avances funcionales para corregir temprano." },
  { title: "Entrega y soporte inicial", text: "Validar escenarios críticos, documentar, desplegar y acompañar la puesta en marcha." },
];

const assurances = [
  { label: "Web", text: "Interfaces modernas, rápidas y adaptadas a escritorio y móvil." },
  { label: "Mobile", text: "Aplicaciones Flutter para experiencias Android, iOS, web y escritorio." },
  { label: "Acceso", text: "Autenticación, roles, RLS y permisos ajustados a cada canal." },
  { label: "Datos", text: "PostgreSQL y Supabase con migraciones, restricciones y consultas mantenibles." },
  { label: "Despliegue", text: "Publicación en Cloudflare con entornos y configuración documentada." },
  { label: "Versionado", text: "GitHub, historial de cambios y backups acordes al riesgo del producto." },
  { label: "Arquitectura", text: "Código modular para que agregar una función no obligue a rehacer el sistema." },
  { label: "Handoff", text: "Documentación técnica y de operación para que el proyecto no dependa de memoria informal." },
];

const quoteFactors = [
  { title: "Landing o sitio institucional", text: "Contenido, cantidad de páginas, identidad visual, formularios e integraciones." },
  { title: "Migración o modernización", text: "Estado del sistema actual, contenido a preservar, redirecciones y riesgo de la transición." },
  { title: "Panel administrativo", text: "Cantidad de roles, módulos, reportes, reglas y fuentes de datos." },
  { title: "MVP", text: "Hipótesis a validar, recorrido central, plataformas y nivel de terminación necesario." },
  { title: "Sistema de gestión", text: "Procesos involucrados, permisos, trazabilidad, migración de datos e integraciones." },
  { title: "Aplicación móvil", text: "Plataformas, funciones nativas, backend, publicación y mantenimiento esperado." },
];

const technologies = [
  { id: "flutter", name: "Flutter", role: "Aplicaciones multiplataforma", description: "Una base de interfaz para experiencias web, Android, escritorio y mobile.", accent: "#02569b" },
  { id: "nextjs", name: "Next.js", role: "Productos web", description: "Interfaces web y paneles internos conectados a datos, APIs y reglas de negocio.", accent: "#202124" },
  { id: "supabase", name: "Supabase", role: "Backend y autenticación", description: "Datos, sesiones, políticas RLS, funciones y almacenamiento sobre PostgreSQL.", accent: "#238a5a" },
  { id: "postgresql", name: "PostgreSQL", role: "Datos consistentes", description: "Modelos, restricciones y consultas para sostener reglas operativas verificables.", accent: "#336791" },
  { id: "astro", name: "Astro", role: "Sitios rápidos", description: "Contenido estructurado y páginas livianas con JavaScript solamente donde aporta valor.", accent: "#d84a05" },
  { id: "tailwind", name: "Tailwind CSS", role: "Sistemas visuales", description: "Interfaces responsivas construidas con reglas consistentes y componentes mantenibles.", accent: "#087f99" },
  { id: "cloudflare", name: "Cloudflare", role: "Entrega y servicios", description: "Pages y Workers para publicar, proteger e integrar productos cerca del usuario.", accent: "#c85f05" },
];

const faqs = [
  {
    question: "¿Qué tipo de proyecto encaja mejor con B-Software?",
    answer:
      "Procesos internos que hoy dependen de planillas, carga repetitiva o herramientas desconectadas; también aplicaciones Flutter, portales de consulta e integraciones con bases de datos y APIs.",
  },
  {
    question: "¿Cómo se define el alcance inicial?",
    answer:
      "Se comienza por el problema más verificable. Antes de desarrollar se acuerdan objetivos, entregables y criterios de aceptación para esa primera etapa.",
  },
  {
    question: "¿El trabajo incluye seguridad y pruebas?",
    answer:
      "Sí. Roles, permisos, reglas de datos y pruebas forman parte de la arquitectura cuando el producto lo requiere; no se dejan únicamente para el final.",
  },
];

const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": "https://bsoftware.pages.dev/#organization",
      name: "B-Software",
      alternateName: "B Software",
      url: "https://bsoftware.pages.dev/",
      logo: {
        "@type": "ImageObject",
        url: "https://bsoftware.pages.dev/brand/logo-b.png",
        contentUrl: "https://bsoftware.pages.dev/brand/logo-b.png",
        width: 1120,
        height: 1120,
      },
      description:
        "Desarrollo de sistemas internos, aplicaciones Flutter e integraciones con foco en procesos, datos, seguridad y operación.",
      email: "gabrielboan14@gmail.com",
      telephone: "+54 9 343 406 4408",
      founder: {
        "@type": "Person",
        "@id": "https://bsoftware.pages.dev/sobre-b-software/#gabriel-boan",
        name: "Gabriel Boan",
        url: "https://bsoftware.pages.dev/sobre-b-software/",
      },
      address: {
        "@type": "PostalAddress",
        addressLocality: "Paraná",
        addressRegion: "Entre Ríos",
        addressCountry: "AR",
      },
      areaServed: ["Argentina", "Remoto"],
      sameAs: ["https://github.com/gabyboan"],
      knowsAbout: [
        "Sistemas internos",
        "Desarrollo Flutter",
        "Supabase",
        "PostgreSQL",
        "Row Level Security",
        "Cloudflare",
        "Automatización",
        "QA funcional",
      ],
      makesOffer: services.map((service) => ({
        "@type": "Offer",
        itemOffered: {
          "@type": "Service",
          name: service.title,
          description: `${service.solves} ${service.includes}`,
        },
      })),
    },
    {
      "@type": "WebSite",
      "@id": "https://bsoftware.pages.dev/#website",
      name: "B-Software",
      alternateName: "B Software",
      url: "https://bsoftware.pages.dev/",
      inLanguage: "es-AR",
      publisher: { "@id": "https://bsoftware.pages.dev/#organization" },
    },
    {
      "@type": "FAQPage",
      mainEntity: faqs.map((faq) => ({
        "@type": "Question",
        name: faq.question,
        acceptedAnswer: { "@type": "Answer", text: faq.answer },
      })),
    },
  ],
};
---

<Layout
  title="Desarrollo de sistemas a medida en Argentina | B-Software"
  description="Sistemas de gestión, desarrollo web, aplicaciones móviles y automatización de procesos para empresas. Software a medida desde Paraná, Argentina."
  navVariant="compact"
>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData)} />

  <main class="home-page">
    <section class="home-hero" aria-labelledby="home-title">
      <div class="hero-copy">
        <p class="eyebrow">Desarrollo de software para empresas · Argentina y remoto</p>
        <h1 id="home-title">Sistemas y aplicaciones para operar mejor.</h1>
        <p class="home-lead">
          Desarrollo sistemas y aplicaciones que reducen tareas manuales, ordenan procesos y hacen que tu negocio pueda operar mejor.
        </p>
        <p class="hero-scope">Sistemas internos · Web y mobile · Automatización · Dashboards · MVPs · Modernización</p>
        <div class="home-actions">
          <a class="button primary" href="#proyectos">Ver proyectos</a>
          <a class="button secondary" href="#contacto">Contar mi proyecto</a>
        </div>
      </div>

      <div class="hero-system" aria-label="Proceso, datos y producto conectados">
        <div class="system-top"><span>B-Software</span><span>Arquitectura operativa</span></div>
        <div class="system-flow">
          <article><small>01</small><strong>Proceso</strong><p>Personas, reglas y tareas reales.</p></article>
          <span class="system-arrow" aria-hidden="true">→</span>
          <article><small>02</small><strong>Datos</strong><p>Una fuente consistente y controlada.</p></article>
          <span class="system-arrow" aria-hidden="true">→</span>
          <article><small>03</small><strong>Producto</strong><p>La interfaz adecuada para operar.</p></article>
        </div>
        <div class="technology-explorer" data-tech-explorer style={`--tech-accent: ${technologies[0].accent}`}>
          <div class="technology-tabs" role="tablist" aria-label="Tecnologías utilizadas en los proyectos">
            {technologies.map((technology, index) => (
              <button
                type="button"
                role="tab"
                id={`technology-tab-${technology.id}`}
                aria-controls="technology-detail"
                aria-selected={index === 0 ? "true" : "false"}
                tabindex={index === 0 ? "0" : "-1"}
                class:list={{ active: index === 0 }}
                data-tech-button
                data-tech-name={technology.name}
                data-tech-role={technology.role}
                data-tech-description={technology.description}
                data-tech-accent={technology.accent}
              >{technology.name}</button>
            ))}
          </div>
          <div id="technology-detail" class="technology-detail" role="tabpanel" aria-labelledby={`technology-tab-${technologies[0].id}`} aria-live="polite">
            <small data-tech-output-role>{technologies[0].role}</small>
            <strong data-tech-output-name>{technologies[0].name}</strong>
            <p data-tech-output-description>{technologies[0].description}</p>
          </div>
        </div>
      </div>
    </section>

    <section id="problemas" class="home-section problems-section" aria-labelledby="problems-title">
      <div class="home-heading">
        <p class="section-index">01 / Qué problemas resuelvo</p>
        <h2 id="problems-title">Cuando la operación crece, las herramientas improvisadas empiezan a costar.</h2>
      </div>
      <div class="problem-grid">
        {problems.map((item, index) => (
          <article>
            <span>{String(index + 1).padStart(2, "0")}</span>
            <h3>{item.title}</h3>
            <p>{item.problem}</p>
            <div><small>Cómo lo abordo</small><strong>{item.solution}</strong></div>
          </article>
        ))}
      </div>
    </section>

    <section id="proyectos" class="home-section case-section" aria-labelledby="cases-title">
      <div class="home-heading">
        <p class="section-index">02 / Casos de estudio</p>
        <h2 id="cases-title">Proyectos reales explicados desde el problema, no desde la lista de tecnologías.</h2>
      </div>

      <div class="case-list">
        <article class="featured-case rrhh-card">
          <div class="case-copy">
            <p class="case-kicker">Sistema interno · Desarrollo activo</p>
            <h3>Un ecosistema para conectar legajos, horas, suplencias y consultas.</h3>
            <p>
              Cuatro productos comparten un núcleo de datos y reglas de acceso. Cada canal resuelve un momento distinto sin duplicar identidad, permisos ni lógica operativa.
            </p>
            <div class="case-business">
              <p><strong>Problema</strong> Información administrativa sensible repartida entre módulos y canales con necesidades de acceso diferentes.</p>
              <p><strong>Solución</strong> Un núcleo compartido con autenticación, roles, RLS, RPCs y productos específicos para cada recorrido.</p>
              <p><strong>Impacto verificable</strong> Legajos, horas, suplencias y consultas se modelan sobre una misma arquitectura documentada.</p>
            </div>
            <dl class="case-facts">
              <div><dt>Funciones</dt><dd>Roles · permisos · trazabilidad</dd></div>
              <div><dt>Canales</dt><dd>Web · escritorio · portal</dd></div>
              <div><dt>Stack</dt><dd>Next.js · Flutter · Supabase</dd></div>
            </dl>
            <a class="case-link" href="/proyectos/gestion-recursos-humanos/">Explorar el caso completo <span aria-hidden="true">↗</span></a>
          </div>
          <div class="case-map" aria-hidden="true">
            <span class="map-node map-legajo">Legajo Digital</span>
            <span class="map-line line-a"></span>
            <span class="map-node map-core">Supabase<small>datos · roles · RPC</small></span>
            <span class="map-line line-b"></span>
            <span class="map-node map-app">Gestión RRHH</span>
            <span class="map-node map-portal">Consulta Horas</span>
          </div>
        </article>

        <article class="featured-case price-card">
          <div class="price-visual" aria-hidden="true">
            <span class="price-orbit orbit-one"></span><span class="price-orbit orbit-two"></span>
            <div class="price-center"><small>Dato</small><strong>Precio + lugar + evidencia</strong></div>
            <span class="price-label label-local">Local verificable</span>
            <span class="price-label label-online">Referencia online</span>
            <span class="price-label label-date">Observación fechada</span>
          </div>
          <div class="case-copy">
            <p class="case-kicker">Tiago Market · Prototipo 0.8</p>
            <h3>Comparar precios sin perder la fuente, la fecha ni el lugar.</h3>
            <p>
              Un producto mobile-first que combina ubicación, sucursales y niveles de certeza para no presentar una referencia online como disponibilidad local confirmada.
            </p>
            <div class="case-business light">
              <p><strong>Problema</strong> Un precio sin contexto puede confundir una referencia online con disponibilidad local en tiempo real.</p>
              <p><strong>Solución</strong> Snapshots auditables, API geográfica y una app Flutter que comunica fuente, vigencia y alcance.</p>
              <p><strong>Impacto verificable</strong> El prototipo conserva observaciones históricas y separa datos verificables, referencias y pendientes de geocodificación.</p>
            </div>
            <dl class="case-facts">
              <div><dt>Funciones</dt><dd>Búsqueda · cercanía · favoritos</dd></div>
              <div><dt>Stack</dt><dd>Flutter · API · Supabase</dd></div>
              <div><dt>Estado</dt><dd>Prototipo · piloto pendiente</dd></div>
            </dl>
            <a class="case-link" href="/proyectos/comparador-geolocalizado-de-precios/">Ver decisiones del producto <span aria-hidden="true">↗</span></a>
          </div>
        </article>

        <article class="development-case">
          <div class="development-copy">
            <p class="case-kicker">Nido · Beta web disponible</p>
            <h3>Nido Inmobiliaria</h3>
            <p><strong>Problema:</strong> explorar publicaciones inmobiliarias requiere un recorrido claro entre búsqueda, filtros, detalle y favoritos.</p>
            <p><strong>Solución:</strong> una aplicación Flutter bilingüe con catálogo demo, filtros, detalle, favoritos y autenticación en validación.</p>
            <p><strong>Estado:</strong> beta técnica web con datos ficticios; Android y flujo completo continúan en preparación.</p>
            <a class="case-link" href="/proyectos/nido-inmobiliaria/">Ver caso de producto <span aria-hidden="true">↗</span></a>
          </div>
          <a
            class="nido-preview"
            href="https://inmobiliaria-app.pages.dev/"
            target="_blank"
            rel="noreferrer"
          >
            <img
              src="/nido-inmobiliaria-beta.png"
              alt="Catálogo real de demostración de Nido Inmobiliaria con propiedades, precios y ubicaciones ficticias"
              width="1440"
              height="1000"
              loading="lazy"
              decoding="async"
            />
            <span>Vista real · Beta web <b aria-hidden="true">↗</b></span>
          </a>
        </article>
      </div>
    </section>

    <section id="servicios" class="home-section services-section" aria-labelledby="services-title">
      <div class="home-heading compact-heading">
        <p class="section-index">03 / Servicios</p>
        <h2 id="services-title">Servicios con un resultado entendible y un alcance que se puede conversar.</h2>
      </div>
      <div class="services-list">
        {services.map((service) => (
          <article>
            <span>{service.number}</span>
            <h3>{service.title}</h3>
            <dl>
              <div><dt>Qué resuelve</dt><dd>{service.solves}</dd></div>
              <div><dt>Puede incluir</dt><dd>{service.includes}</dd></div>
              <div><dt>Ideal para</dt><dd>{service.ideal}</dd></div>
            </dl>
            <a href="#contacto">Consultar por este servicio <span aria-hidden="true">→</span></a>
          </article>
        ))}
      </div>
    </section>

    <section id="metodo" class="home-section method-section" aria-labelledby="method-title">
      <div class="method-intro">
        <p class="section-index">04 / Proceso de trabajo</p>
        <h2 id="method-title">Alcance definido, hitos visibles y entregables concretos.</h2>
        <p>El objetivo es evitar presupuestos ambiguos: cada etapa tiene algo que revisar, validar y decidir antes de avanzar.</p>
      </div>
      <ol class="method-list">
        {workSteps.map((step, index) => (
          <li><span>{String(index + 1).padStart(2, "0")}</span><div><h3>{step.title}</h3><p>{step.text}</p></div></li>
        ))}
      </ol>
    </section>

    <section id="calidad" class="trust-section" aria-labelledby="trust-title">
      <div class="trust-inner">
        <div class="trust-heading">
          <p class="section-index">05 / Base técnica</p>
          <h2 id="trust-title">La tecnología respalda al servicio; no reemplaza el problema de negocio.</h2>
          <p>Estas capacidades permiten entregar productos seguros, desplegables y mantenibles cuando el proyecto las necesita.</p>
        </div>
        <div class="trust-list">
          {assurances.map((item) => (
            <article><span>{item.label}</span><p>{item.text}</p></article>
          ))}
        </div>
      </div>
    </section>

    <section id="cotizacion" class="home-section quote-section" aria-labelledby="quote-title">
      <div class="quote-intro">
        <p class="section-index">06 / Cómo se cotiza</p>
        <h2 id="quote-title">El valor depende del alcance, no de una etiqueta.</h2>
        <p>
          No publico precios rígidos porque dos proyectos con el mismo nombre pueden requerir esfuerzos muy distintos. La propuesta se arma según complejidad, integraciones, estado de los datos, plazos y nivel de acompañamiento.
        </p>
        <div class="quote-note">
          <strong>La primera conversación sirve para ubicar el proyecto.</strong>
          <span>Después recibís una propuesta por etapas, con entregables y supuestos explícitos. Si todavía hay demasiadas incógnitas, se cotiza primero un relevamiento.</span>
        </div>
      </div>
      <div class="quote-grid">
        {quoteFactors.map((item) => (
          <article><h3>{item.title}</h3><p>{item.text}</p></article>
        ))}
      </div>
    </section>

    <section class="home-section about-strip" aria-labelledby="about-title">
      <div>
        <p class="section-index">07 / Detrás de B-Software</p>
        <h2 id="about-title">Criterio técnico con una conversación directa.</h2>
      </div>
      <div>
        <p>
          B-Software es la marca profesional independiente de Gabriel Boan, desarrollador de Paraná, Entre Ríos. El foco está en entender procesos y convertirlos en software mantenible, explicable y útil para quienes lo operan.
        </p>
        <a class="text-link" href="/sobre-b-software/">Conocer el enfoque y la experiencia <span aria-hidden="true">→</span></a>
      </div>
    </section>

    <section id="faq" class="home-section faq-clean" aria-labelledby="faq-title">
      <div class="home-heading compact-heading">
        <p class="section-index">08 / Preguntas frecuentes</p>
        <h2 id="faq-title">Lo esencial antes de empezar.</h2>
      </div>
      <div class="faq-clean-list">
        {faqs.map((faq, index) => (
          <details>
            <summary><span>{String(index + 1).padStart(2, "0")}</span>{faq.question}</summary>
            <p>{faq.answer}</p>
          </details>
        ))}
      </div>
    </section>

    <section id="contacto" class="home-contact" aria-labelledby="contact-title">
      <div class="contact-intro">
        <p class="section-index">Contame qué necesitás resolver</p>
        <h2 id="contact-title">Una buena propuesta empieza con contexto.</h2>
        <p>Completá lo que ya sabés. El formulario prepara un mensaje y abre WhatsApp; los datos no se guardan en este sitio ni se envían sin tu confirmación.</p>
        <div class="contact-direct">
          <a href="mailto:gabrielboan14@gmail.com">gabrielboan14@gmail.com</a>
          <a href="https://wa.me/5493434064408" target="_blank" rel="noreferrer">WhatsApp directo ↗</a>
        </div>
      </div>
      <form id="project-contact-form" class="project-form" novalidate>
        <div class="form-grid">
          <label>Nombre <input type="text" name="name" autocomplete="name" required /></label>
          <label>Empresa o actividad <input type="text" name="business" autocomplete="organization" required /></label>
          <label class="form-wide">Email o WhatsApp <input type="text" name="contact" autocomplete="email" required aria-describedby="contact-help" /></label>
          <small id="contact-help" class="form-help form-wide">Usaré este dato únicamente para responder tu consulta.</small>
          <label class="form-wide">Qué necesitás resolver <textarea name="need" rows="5" minlength="20" required placeholder="Contame cómo trabajan hoy, qué se complica y qué resultado esperás."></textarea></label>
          <label>Tipo de proyecto
            <select name="type" required>
              <option value="">Seleccionar</option>
              <option>Sistema interno</option><option>Aplicación web</option><option>App móvil</option>
              <option>Migración web</option><option>Automatización</option><option>MVP</option><option>Otro</option>
            </select>
          </label>
          <label>Presupuesto estimado <span>(opcional)</span><input type="text" name="budget" placeholder="Rango o referencia" /></label>
          <label class="form-wide">Plazo deseado <span>(opcional)</span><input type="text" name="timeline" placeholder="Por ejemplo: este trimestre" /></label>
        </div>
        <p class="form-status" data-form-status role="status" aria-live="polite"></p>
        <button class="button contact-submit" type="submit">Continuar en WhatsApp <span aria-hidden="true">↗</span></button>
      </form>
    </section>
  </main>

  <footer class="site-footer">
    <div class="footer-inner">
      <a class="footer-brand" href="/">
        <span class="footer-logo"><img src="/brand/logo-b-96.webp" alt="" width="96" height="96" loading="lazy" /></span>
        <span><strong>B Software</strong><small>Sistemas internos, Flutter e integraciones</small></span>
      </a>
      <nav class="footer-links" aria-label="Enlaces secundarios">
        <a href="/sobre-b-software/">Sobre B-Software</a>
        <a href="/#proyectos">Casos</a>
        <a href="https://github.com/gabyboan" target="_blank" rel="noreferrer">GitHub</a>
        <a href="/#contacto">Contacto</a>
      </nav>
    </div>
  </footer>

  <script>
    document.querySelectorAll<HTMLElement>("[data-tech-explorer]").forEach((explorer) => {
      const buttons = Array.from(explorer.querySelectorAll<HTMLButtonElement>("[data-tech-button]"));
      const name = explorer.querySelector("[data-tech-output-name]");
      const role = explorer.querySelector("[data-tech-output-role]");
      const description = explorer.querySelector("[data-tech-output-description]");
      const detail = explorer.querySelector("[role='tabpanel']");

      const activate = (button: HTMLButtonElement) => {
        buttons.forEach((item) => {
          item.classList.toggle("active", item === button);
          item.setAttribute("aria-selected", String(item === button));
          item.setAttribute("tabindex", item === button ? "0" : "-1");
        });
        if (name) name.textContent = button.dataset.techName ?? "";
        if (role) role.textContent = button.dataset.techRole ?? "";
        if (description) description.textContent = button.dataset.techDescription ?? "";
        if (detail) detail.setAttribute("aria-labelledby", button.id);
        explorer.style.setProperty("--tech-accent", button.dataset.techAccent ?? "#263fbd");
      };

      buttons.forEach((button, index) => {
        button.addEventListener("pointerenter", () => activate(button));
        button.addEventListener("focus", () => activate(button));
        button.addEventListener("click", () => activate(button));
        button.addEventListener("keydown", (event) => {
          if (!["ArrowLeft", "ArrowRight"].includes(event.key)) return;
          event.preventDefault();
          const direction = event.key === "ArrowRight" ? 1 : -1;
          const next = buttons[(index + direction + buttons.length) % buttons.length];
          if (next instanceof HTMLButtonElement) next.focus();
        });
      });
    });

    const contactForm = document.querySelector("#project-contact-form");
    if (contactForm instanceof HTMLFormElement) {
      contactForm.addEventListener("submit", (event) => {
        event.preventDefault();
        const status = contactForm.querySelector("[data-form-status]");
        if (!contactForm.checkValidity()) {
          contactForm.reportValidity();
          if (status) status.textContent = "Revisá los campos obligatorios antes de continuar.";
          return;
        }

        const values = Object.fromEntries(new FormData(contactForm).entries());
        const optional = (value: FormDataEntryValue | undefined) => typeof value === "string" && value.trim() ? value.trim() : "Sin definir";
        const message = [
          "Hola Gabriel, quiero conversar sobre un proyecto.",
          "",
          `Nombre: ${optional(values.name)}`,
          `Empresa o actividad: ${optional(values.business)}`,
          `Contacto: ${optional(values.contact)}`,
          `Tipo de proyecto: ${optional(values.type)}`,
          `Qué necesito resolver: ${optional(values.need)}`,
          `Presupuesto estimado: ${optional(values.budget)}`,
          `Plazo deseado: ${optional(values.timeline)}`,
        ].join("\n");

        if (status) status.textContent = "Abriendo WhatsApp para que revises y envíes el mensaje.";
        window.open(`https://wa.me/5493434064408?text=${encodeURIComponent(message)}`, "_blank", "noopener,noreferrer");
      });
    }
  </script>
</Layout>

<style>
  .home-page { background: #f5f4f0; color: #171815; }
  .home-hero, .home-section, .home-contact { max-width: 1180px; margin: 0 auto; padding-inline: 20px; }
  .home-hero { min-height: 690px; padding-top: 88px; padding-bottom: 78px; display: grid; grid-template-columns: 1.08fr .92fr; gap: 70px; align-items: center; }
  .home-hero h1 { max-width: 780px; font-size: clamp(3.5rem, 6.4vw, 6.8rem); line-height: .92; letter-spacing: -.06em; }
  .home-lead { max-width: 700px; margin-top: 28px; color: #666963; font-size: 1.18rem; line-height: 1.75; }
  .hero-scope { max-width: 680px; margin-top: 18px; color: #454842; font-size: .82rem; font-weight: 850; letter-spacing: .035em; line-height: 1.7; text-transform: uppercase; }
  .home-actions { margin-top: 34px; display: flex; flex-wrap: wrap; align-items: center; gap: 22px; }
  .text-link, .case-link { display: inline-flex; align-items: center; gap: 8px; color: #263fbd; font-weight: 900; }
  .text-link:hover, .case-link:hover { text-decoration: underline; text-underline-offset: 5px; }
  .hero-system { min-height: 530px; display: flex; flex-direction: column; justify-content: space-between; border: 1px solid #ceccc5; border-radius: 10px; padding: 22px; background: #fbfaf7; box-shadow: 0 24px 60px rgb(23 24 21 / .08); }
  .system-top { display: flex; justify-content: space-between; gap: 12px; color: #666963; font-size: .7rem; font-weight: 850; letter-spacing: .08em; text-transform: uppercase; }
  .system-flow { display: grid; grid-template-columns: 1fr auto 1fr auto 1fr; gap: 8px; align-items: center; }
  .system-flow article { min-width: 0; border-top: 2px solid #263fbd; padding-top: 12px; }
  .system-flow small { color: #263fbd; font-weight: 900; }
  .system-flow strong, .system-flow p { display: block; }
  .system-flow strong { margin-top: 18px; font-size: 1.05rem; }
  .system-flow p { margin-top: 8px; color: #666963; font-size: .76rem; line-height: 1.5; }
  .system-arrow { color: #989a94; }
  .technology-explorer { border-top: 1px solid #d6d4cd; padding-top: 16px; }
  .technology-tabs { display: flex; flex-wrap: wrap; gap: 7px; }
  .technology-tabs button { border: 1px solid #d6d4cd; border-radius: 999px; padding: 6px 9px; color: #6d706a; background: #fff; font: inherit; font-size: .65rem; font-weight: 850; letter-spacing: .05em; text-transform: uppercase; cursor: pointer; transition: border-color .16s ease, color .16s ease, background .16s ease; }
  .technology-tabs button:hover, .technology-tabs button:focus-visible, .technology-tabs button.active { border-color: var(--tech-accent); color: var(--tech-accent); background: color-mix(in srgb, var(--tech-accent) 7%, white); outline: none; }
  .technology-tabs button:focus-visible { box-shadow: 0 0 0 3px color-mix(in srgb, var(--tech-accent) 20%, transparent); }
  .technology-detail { min-height: 82px; margin-top: 14px; border-left: 2px solid var(--tech-accent); padding-left: 12px; }
  .technology-detail small, .technology-detail strong { display: block; }.technology-detail small { color: var(--tech-accent); font-size: .62rem; font-weight: 900; letter-spacing: .07em; text-transform: uppercase; }.technology-detail strong { margin-top: 4px; font-size: .92rem; }.technology-detail p { margin-top: 5px; color: #6d706a; font-size: .73rem; line-height: 1.45; }

  .home-section { padding-top: 104px; padding-bottom: 104px; }
  .home-heading { display: grid; grid-template-columns: .32fr .68fr; gap: 48px; align-items: start; }
  .home-heading h2, .method-intro h2, .trust-heading h2, .about-strip h2, .home-contact h2 { margin-top: 0; font-size: clamp(2.3rem, 4.6vw, 4.5rem); letter-spacing: -.04em; }
  .section-index { color: #263fbd; font-size: .74rem; font-weight: 950; letter-spacing: .11em; text-transform: uppercase; }
  .problems-section { border-top: 1px solid #d2d1cb; }
  .problem-grid { margin-top: 54px; display: grid; grid-template-columns: repeat(3, 1fr); border-top: 1px solid #c9c7c0; border-left: 1px solid #c9c7c0; }
  .problem-grid article { min-height: 330px; display: flex; flex-direction: column; border-right: 1px solid #c9c7c0; border-bottom: 1px solid #c9c7c0; padding: 26px; background: rgb(255 255 255 / .45); }
  .problem-grid article > span { color: #263fbd; font-size: .7rem; font-weight: 900; }
  .problem-grid h3 { margin-top: 38px; font-size: 1.35rem; }
  .problem-grid article > p { margin-top: 12px; color: #666963; line-height: 1.65; }
  .problem-grid article > div { margin-top: auto; border-top: 1px solid #d2d1cb; padding-top: 18px; }
  .problem-grid small, .problem-grid strong { display: block; }.problem-grid small { color: #666963; font-size: .65rem; font-weight: 900; letter-spacing: .08em; text-transform: uppercase; }.problem-grid strong { margin-top: 8px; color: #242622; line-height: 1.55; }
  .case-list { margin-top: 54px; display: grid; gap: 22px; }
  .featured-case { min-height: 570px; display: grid; grid-template-columns: 1.02fr .98fr; overflow: hidden; border-radius: 10px; }
  .rrhh-card { color: #f5f4f0; background: #111214; }
  .case-copy { display: flex; flex-direction: column; padding: 42px; }
  .case-kicker { color: #a89bf7; font-size: .72rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }
  .featured-case h3 { margin-top: 22px; max-width: 680px; font-size: clamp(2rem, 4vw, 3.8rem); letter-spacing: -.04em; line-height: 1; }
  .case-copy > p:not(.case-kicker) { margin-top: 24px; max-width: 660px; color: rgb(255 255 255 / .66); line-height: 1.75; }
  .case-business { margin-top: 24px; display: grid; gap: 10px; }
  .case-business p { color: rgb(255 255 255 / .68); font-size: .84rem; line-height: 1.55; }
  .case-business strong { margin-right: 6px; color: #f5f4f0; }
  .case-business.light p { color: #666963; }.case-business.light strong { color: #252622; }
  .case-facts { margin: 34px 0 0; display: grid; grid-template-columns: repeat(3, 1fr); border-top: 1px solid rgb(255 255 255 / .17); }
  .case-facts div { padding: 16px 12px 0 0; }
  .case-facts dt { color: rgb(255 255 255 / .46); font-size: .66rem; font-weight: 850; text-transform: uppercase; }
  .case-facts dd { margin: 6px 0 0; font-size: .82rem; font-weight: 800; }
  .case-link { margin-top: auto; padding-top: 30px; color: #d1ccff; }
  .case-map { position: relative; margin: 22px 22px 22px 0; overflow: hidden; border: 1px solid rgb(255 255 255 / .1); border-radius: 8px; background-color: #15171b; background-image: radial-gradient(rgb(255 255 255 / .08) 1px, transparent 1px); background-size: 22px 22px; }
  .map-node { position: absolute; z-index: 2; min-width: 130px; border: 1px solid rgb(168 155 247 / .45); border-radius: 7px; padding: 13px; background: #1b1d23; color: #f5f4f0; font-size: .75rem; font-weight: 850; }
  .map-node small { display: block; margin-top: 6px; color: #888d98; font-size: .6rem; font-weight: 600; }
  .map-legajo { top: 16%; left: 8%; }.map-core { top: 42%; left: 37%; border-color: #a89bf7; }.map-app { right: 8%; top: 18%; }.map-portal { right: 8%; bottom: 15%; border-color: #f0b36c; }
  .map-line { position: absolute; z-index: 1; height: 1px; background: #a89bf7; transform-origin: left center; }
  .line-a { top: 29%; left: 28%; width: 31%; transform: rotate(28deg); }.line-b { top: 53%; left: 55%; width: 31%; transform: rotate(-28deg); }
  .price-card { border: 1px solid #d2d1cb; background: #fff; }
  .price-card .case-kicker { color: #147843; }
  .price-card .case-copy > p:not(.case-kicker) { color: #666963; }
  .price-card .case-facts { border-color: #d8d7d1; }
  .price-card .case-facts dt { color: #777771; }.price-card .case-facts dd { color: #252622; }
  .price-card .case-link { color: #147843; }
  .price-visual { position: relative; min-height: 480px; margin: 22px 0 22px 22px; overflow: hidden; border-radius: 8px; background: #eef5ef; }
  .price-center { position: absolute; top: 50%; left: 50%; z-index: 2; width: 190px; border: 1px solid #abcbb5; border-radius: 50%; padding: 48px 24px; background: #fff; text-align: center; transform: translate(-50%, -50%); }
  .price-center small, .price-center strong { display: block; }.price-center small { color: #147843; font-weight: 900; text-transform: uppercase; }.price-center strong { margin-top: 8px; }
  .price-orbit { position: absolute; top: 50%; left: 50%; border: 1px solid #b9d4c1; border-radius: 50%; transform: translate(-50%, -50%); }.orbit-one { width: 330px; height: 330px; }.orbit-two { width: 460px; height: 460px; border-style: dashed; }
  .price-label { position: absolute; border: 1px solid #c6daca; border-radius: 999px; padding: 8px 10px; color: #285c3b; background: rgb(255 255 255 / .9); font-size: .7rem; font-weight: 850; }.label-local { top: 13%; left: 12%; }.label-online { right: 8%; top: 34%; }.label-date { bottom: 12%; left: 18%; }
  .development-case { min-height: 310px; display: grid; grid-template-columns: 1.12fr .88fr; overflow: hidden; border: 1px solid #cbd0cb; border-radius: 10px; background: #eef1ee; }
  .development-copy { display: flex; flex-direction: column; padding: 34px; }.development-copy .case-kicker { color: #566a5d; }.development-copy h3 { margin-top: 14px; font-size: clamp(1.9rem, 3.2vw, 3rem); letter-spacing: -.04em; }.development-copy > p:not(.case-kicker) { max-width: 650px; margin-top: 11px; color: #62665f; line-height: 1.6; }.development-copy > p strong { color: #2e3c32; }.development-copy .case-link { margin-top: auto; color: #46594c; }
  .nido-preview { position: relative; min-width: 0; margin: 18px; overflow: hidden; border: 1px solid #bdc5bf; border-radius: 8px; background: #f8f7f3; }.nido-preview img { width: 100%; height: 100%; object-fit: cover; object-position: center; }.nido-preview > span { position: absolute; right: 12px; bottom: 12px; border: 1px solid rgb(27 36 30 / .16); border-radius: 999px; padding: 7px 10px; color: #1b241e; background: rgb(255 255 255 / .94); font-size: .66rem; font-weight: 900; letter-spacing: .04em; text-transform: uppercase; }.nido-preview:hover, .nido-preview:focus-visible { border-color: #566a5d; outline: none; }.nido-preview:focus-visible { box-shadow: 0 0 0 3px rgb(86 106 93 / .2); }

  .compact-heading { grid-template-columns: .35fr .65fr; }
  .services-list { margin-top: 52px; display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
  .services-list article { min-height: 480px; display: flex; flex-direction: column; border: 1px solid #c9c7c0; border-radius: 10px; padding: 28px; background: #fbfaf7; }
  .services-list article:last-child { grid-column: 1 / -1; min-height: 390px; }
  .services-list > article > span { color: #263fbd; font-size: .72rem; font-weight: 900; }
  .services-list h3 { margin-top: 36px; font-size: 1.65rem; }
  .services-list dl { margin: 28px 0 0; display: grid; gap: 16px; }
  .services-list dl div { border-top: 1px solid #d8d6cf; padding-top: 13px; }
  .services-list dt { color: #666963; font-size: .65rem; font-weight: 900; letter-spacing: .08em; text-transform: uppercase; }
  .services-list dd { margin: 6px 0 0; color: #555852; line-height: 1.58; }
  .services-list a { margin-top: auto; padding-top: 28px; color: #263fbd; font-weight: 900; }
  .services-list a:hover { text-decoration: underline; text-underline-offset: 5px; }

  .method-section { display: grid; grid-template-columns: .85fr 1.15fr; gap: 86px; border-top: 1px solid #d2d1cb; }
  .method-intro > p:last-child { margin-top: 24px; max-width: 520px; color: #666963; line-height: 1.75; }
  .method-list { margin: 0; padding: 0; list-style: none; }
  .method-list li { display: grid; grid-template-columns: 48px 1fr; gap: 20px; border-top: 1px solid #c9c7c0; padding: 24px 0; }.method-list li:last-child { border-bottom: 1px solid #c9c7c0; }
  .method-list li > span { color: #263fbd; font-size: .74rem; font-weight: 900; }.method-list h3 { font-size: 1.25rem; }.method-list p { margin-top: 8px; color: #666963; line-height: 1.65; }

  .trust-section { color: #f5f4f0; background: #111214; }
  .trust-inner { max-width: 1180px; margin: 0 auto; padding: 100px 20px; display: grid; grid-template-columns: .9fr 1.1fr; gap: 90px; }
  .trust-heading .section-index { color: #a89bf7; }.trust-heading > p:last-child { margin-top: 24px; color: rgb(255 255 255 / .62); line-height: 1.75; }
  .trust-list { display: grid; grid-template-columns: 1fr 1fr; border-top: 1px solid rgb(255 255 255 / .18); }
  .trust-list article { min-height: 170px; border-bottom: 1px solid rgb(255 255 255 / .18); padding: 22px; }.trust-list article:nth-child(odd) { border-right: 1px solid rgb(255 255 255 / .18); }
  .trust-list span { color: #a89bf7; font-size: .7rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }.trust-list p { margin-top: 20px; color: rgb(255 255 255 / .74); line-height: 1.65; }

  .quote-section { display: grid; grid-template-columns: .82fr 1.18fr; gap: 84px; border-bottom: 1px solid #d2d1cb; }
  .quote-intro h2 { margin-top: 18px; font-size: clamp(2.3rem, 4.6vw, 4.5rem); letter-spacing: -.04em; }
  .quote-intro > p:not(.section-index) { margin-top: 24px; color: #5f625c; line-height: 1.8; }
  .quote-note { margin-top: 32px; border-left: 3px solid #263fbd; padding: 5px 0 5px 18px; }
  .quote-note strong, .quote-note span { display: block; }.quote-note span { margin-top: 8px; color: #666963; font-size: .9rem; line-height: 1.65; }
  .quote-grid { border-top: 1px solid #c9c7c0; }
  .quote-grid article { display: grid; grid-template-columns: .7fr 1.3fr; gap: 24px; border-bottom: 1px solid #c9c7c0; padding: 23px 0; }
  .quote-grid h3 { font-size: 1rem; }.quote-grid p { color: #666963; font-size: .92rem; line-height: 1.65; }

  .about-strip { display: grid; grid-template-columns: .9fr 1.1fr; gap: 90px; border-bottom: 1px solid #d2d1cb; }
  .about-strip > div:last-child { padding-top: 26px; }.about-strip > div:last-child p { color: #5f625c; font-size: 1.08rem; line-height: 1.85; }.about-strip .text-link { margin-top: 26px; }
  .faq-clean-list { margin-top: 52px; border-top: 1px solid #c9c7c0; }
  .faq-clean-list details { border-bottom: 1px solid #c9c7c0; }.faq-clean-list summary { min-height: 76px; display: flex; align-items: center; gap: 24px; cursor: pointer; font-size: 1.08rem; font-weight: 850; }.faq-clean-list summary span { color: #263fbd; font-size: .7rem; }.faq-clean-list details p { max-width: 790px; padding: 0 0 26px 48px; color: #666963; line-height: 1.75; }

  .home-contact { max-width: 1180px; margin-bottom: 70px; display: grid; grid-template-columns: .78fr 1.22fr; gap: 64px; align-items: start; border-radius: 10px; padding: 64px; color: #fff; background: #263fbd; }
  .home-contact .section-index { color: #d1ccff; }.home-contact h2 { margin-top: 18px; max-width: 800px; }
  .contact-intro > p:not(.section-index) { margin-top: 22px; color: rgb(255 255 255 / .72); line-height: 1.75; }
  .contact-direct { margin-top: 28px; display: grid; gap: 8px; }.contact-direct a { width: fit-content; color: #fff; font-weight: 800; text-decoration: underline; text-decoration-color: rgb(255 255 255 / .35); text-underline-offset: 4px; }
  .project-form { border-radius: 9px; padding: 26px; color: #171815; background: #fff; }
  .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 18px 14px; }
  .project-form label { display: grid; gap: 7px; color: #353732; font-size: .8rem; font-weight: 850; }.project-form label > span { color: #777771; font-weight: 600; }
  .project-form input, .project-form textarea, .project-form select { width: 100%; border: 1px solid #c9c7c0; border-radius: 7px; padding: 11px 12px; color: #171815; background: #fff; font: inherit; font-weight: 500; }
  .project-form textarea { resize: vertical; }.project-form input:focus, .project-form textarea:focus, .project-form select:focus { border-color: #263fbd; outline: 3px solid rgb(38 63 189 / .14); }
  .form-wide { grid-column: 1 / -1; }.form-help { margin-top: -12px; color: #777771; font-size: .72rem; }
  .form-status { min-height: 22px; margin-top: 16px; color: #4d504a; font-size: .78rem; }
  .contact-submit { width: 100%; margin-top: 8px; border: 0; color: #fff; background: #111214; cursor: pointer; font: inherit; }

  @media (max-width: 900px) {
    .home-hero, .home-heading, .featured-case, .development-case, .method-section, .trust-inner, .quote-section, .about-strip, .home-contact { grid-template-columns: 1fr; }
    .home-hero { min-height: auto; gap: 48px; padding-top: 56px; }.hero-system { min-height: 500px; }
    .case-copy { padding: 30px; }.case-map { min-height: 430px; margin: 0 22px 22px; }.price-visual { min-height: 430px; margin: 22px; order: 2; }
    .price-card .case-copy { order: 1; }.nido-preview { min-height: 250px; }.problem-grid { grid-template-columns: 1fr 1fr; }
    .method-section, .trust-inner, .quote-section, .about-strip { gap: 46px; }.home-contact { margin-inline: 14px; padding: 46px; }
  }

  @media (max-width: 560px) {
    .home-hero, .home-section, .trust-inner { padding-inline: 14px; }.home-hero h1 { font-size: clamp(3rem, 15vw, 4.2rem); }
    .system-flow { grid-template-columns: 1fr; gap: 16px; }.system-arrow { display: none; }.hero-system { min-height: 690px; }.technology-detail { min-height: 98px; }
    .home-section { padding-top: 76px; padding-bottom: 76px; }.case-copy { padding: 24px; }.case-facts { grid-template-columns: 1fr; }.case-facts div { padding-bottom: 10px; }
    .case-map, .price-visual { min-height: 360px; margin: 0 14px 14px; }.price-visual { margin-top: 14px; }.nido-preview { min-height: 220px; margin: 0 14px 14px; }.orbit-two { width: 390px; height: 390px; }.orbit-one { width: 270px; height: 270px; }
    .problem-grid, .services-list, .form-grid { grid-template-columns: 1fr; }.problem-grid article { min-height: 280px; }.services-list article, .services-list article:last-child { grid-column: auto; min-height: auto; }.services-list a { margin-top: 36px; }.form-wide { grid-column: auto; }.quote-grid article { grid-template-columns: 1fr; gap: 8px; }
    .trust-list { grid-template-columns: 1fr; }.trust-list article:nth-child(odd) { border-right: 0; }.trust-list article { min-height: auto; padding-inline: 0; }
    .faq-clean-list details p { padding-left: 0; }.home-contact { margin-bottom: 30px; padding: 42px 20px; }.project-form { padding: 20px; }
  }
</style>
